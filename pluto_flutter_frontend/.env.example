# Backend base URL for APIs (replace mock services when backend is ready)
API_BASE_URL=https://mock.pluto.local
# WebSocket URL for real-time alerts (not implemented in mock)
WS_URL=wss://mock.pluto.local/ws
