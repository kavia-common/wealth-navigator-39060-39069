# Example environment configuration for Pluto Flutter app
# Copy to .env and adjust values for your environment.
API_BASE_URL=https://mock.pluto.local
WS_URL=wss://mock.pluto.local/ws

# Yahoo Finance integration uses public endpoints on the client side and
# does not require extra keys. For production, proxy via backend.
